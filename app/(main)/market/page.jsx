'use client';

import MarketPageContent from '@/app/components/pages/MarketPageContent';

// Route content for `/market`. Renders inside the persistent AppShell chrome.
export default function MarketPage() {
  return <MarketPageContent />;
}
